#include <stdio.h>

void main(){
    int size,target;
    scanf("%d", &size);
    int arr[size];
    for (int i = 0; i < size; i++) {
        scanf("%d", &arr[i]);
    }
    int sums=0;
    for (int i=0;i<size;i++){  
        sums+=arr[i] 
        printf("%d ", sums;)
    }
    
    }